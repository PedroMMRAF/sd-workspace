# Maven compile
mvn clean compile assembly:single docker:build

# Run Users Server
docker run --rm -it -h feeds.nova --name feeds.nova --network sdnet -p 8080:8080 sd2223-trab1-60694-60814 java -cp sd2223.jar trab1.server.users.UsersServer nova

# Run Users CreateUserClient
docker run --rm -it --name users --network sdnet sd2223-trab1-60694-60814 java -cp sd2223.jar trab1.client.users.CreateUserClient nova john 12345 John

# Run tests
.\test-sd-tp2.bat -image sd2223-trab1-60694-60814

# Kill container
docker kill <docker-name>
